# Decision log

Settled decisions, newest first. Append a dated entry when something gets decided; correct an old entry by appending, not rewriting. Do not relitigate these silently: if you believe one is wrong, say so to the owner with evidence.

## 2026-08-13: internationalization shipped for eight locales

The direction entry below became real the same day: umbrella, provider and language pages in en, es, zh, hi, fr, bn, pt and ru (1,180+ prerendered pages), full hreflang/sitemap reciprocity, suggestion banner, footer switcher, localized search terms. Hard-won build lesson recorded for the next agent: the proxy may import only i18n/routing.ts; navigation helpers and the request config (which reads the locale from next/root-params) live in separate modules because bundling them into the middleware both breaks the build and silently turns every page dynamic. Translations are AI-written and pending the owner's review, as decided. The peer reviewer's post-ship pass (gpt-5.6-sol, resumed session) surfaced seven real defects, all fixed and verified the same day; two glossary rules were tightened by it: Polly's engine identifiers (standard, neural, generative, long-form) stay verbatim English inside translated prose exactly as they do on the tiles, and grammatical composition (measure words, case) always lives inside per-locale messages, never in code that concatenates a count with a word.

## 2026-08-13: search stays UI-only and becomes complete; fuzzy waits for evidence

Search never touches an API: the catalog is already client-loaded and stays that way. The fix for "can't search gender/tier/styles/anything future": one memoized search document per voice built automatically from every field and trait, so new characteristics are searchable by construction; queries tokenize and AND together ("female child" works); diacritics are stripped on both sides; a pasted full voice id resolves to its exact row. Display stays grouped by language. Fuse.js was considered (the owner's other product uses it) and deliberately deferred: the search_used analytics will show real zero-result queries, and the fuzzy layer gets added if typos show up in evidence, not speculation. Peer-reviewed by kiro gpt-5.6-sol.

## 2026-08-13: internationalization direction (owner decisions)

Owner decisions, recorded before the build: browser-language handling is a one-tap suggestion banner in the visitor's language, never a hard redirect (crawler-safe, Google-guidance-aligned). Launch locales are the world's most spoken languages minus right-to-left: es, zh, hi, fr, bn, pt, ru beside default en; Arabic and other RTL wait for a dedicated RTL pass the owner will review jointly. English URLs stay byte-for-byte unchanged (as-needed locale prefix; /en variants redirect to clean paths). next-intl is the machinery (pinned after a compat spike): plurals, interpolated counts and rich text outgrow a hand-rolled t(). Protected names never translate: voice names, family/engine names, provider names, Speakshelf, AI TTS Microservice, codes and ids; language names localize via Intl. llms.txt and the OG card stay English. Translations are AI-written and honestly marked owner-review-pending; expansion beyond the set is one locale at a time. The peer reviewer is kiro gpt-5.6-sol, session 1ff1dd9c-49b6-48f7-a499-58495f3c2f14, resumed with --resume-id for continuity until this work completes.

## 2026-08-13: sample audio hosts are an allowlist, with R2 pre-added

Playback is host-agnostic everywhere except the production CSP, which allowlists where audio may load from. The owner confirmed samples will soon also live on Cloudflare R2, so both R2 host shapes are pre-added (`*.r2.dev` public buckets, `*.r2.cloudflarestorage.com` presigned URLs, per Cloudflare's docs). Custom domains cannot be predicted, so `/api/sample` carries a tripwire: the first sample URL from a host outside the allowlist logs a loud server warning naming the host, before visitors ever hit the CSP wall.

## 2026-08-13: trait tags, a first-play quirk toast, and a cosmetic consent bar

Three owner calls in one round. Child voices show as a gray CHILD tag beside the family tag, not a parenthetical in the gender cell; trait tags are exempt from the mobile tag-hide because a rare trait matters more than a repeated family name. The Gemini accent note additionally appears as a one-time-per-session toast on the first play of a noted family's voice, because the top-of-list note is off screen once a reader scrolls into a long list; the toast is the Carbon notification shape, auto-dismisses, and never repeats in a session. A consent bar mirrors the tts-microutil V2 posture: shown until the visitor picks Accept all or Necessary only, the true choice is stored, and nothing gates on it yet; `applyConsent` in lib/consent.ts is the single seam where the owner's legal team's rules will land, and there is deliberately no settings link to reopen it (resetConsent exists in code for that future).

## 2026-08-13: voice traits are retained, not dropped

The owner caught the packed format silently dropping voice characteristics: Amazon Polly's child voices (Ivy, Justin, Kevin, marked age "child" by the API and "Female (child)" in AWS's own table) rendered as plain female or male here. The tuple gained an optional seventh slot carrying non-style characteristics keyed exactly as the API keys them; it turned out to also preserve per-voice pitch on 2,610 Google rows. Displayed today: age only ("female (child)" in the voice row, searchable). Pitch, accent, use_case and roles are retained in data and the catalog API but not yet surfaced; Azure's richer characteristics will flow through the same slot when that provider lands. Principle: the pack never discards a characteristic the API provides, and display grows deliberately, not automatically.

## 2026-08-13: analytics are always-on pass-through; the owner's legal owns the posture

Mixpanel (autocapture, 100 percent session replay) and GA4 run for every visitor with no consent gate, by the owner's explicit decision; their legal team is working the compliance posture and will dictate whatever consent surface follows. Costs flagged and accepted: full replay is a standing data-volume commitment, and ignore_dnt is on. Implementation follows the proven tts-microutil pattern where it earns its keep (single vendor-boundary module, query-stripped page locations) and skips its scale (no consent UI, no governance registry, no identity hashing: this site has no accounts). The GA measurement id supplied was numeric; GA4 web streams use a G- prefixed id, so gtag loads but sends nothing until the owner swaps the real id into .env (verified live: zero collect beacons).

## 2026-08-13: security headers with a deliberately boring CSP

Every route ships X-Frame-Options DENY, nosniff, strict-origin-when-cross-origin, COOP same-origin, and a Permissions-Policy denying microphone, camera and geolocation; production adds HSTS and a CSP. The CSP keeps `script-src 'unsafe-inline'`: it is load-bearing for the pages' inline JSON-LD blocks, so it must not be misread as analytics-only and "tightened" away later. Known accepted gap, recorded on the reviewer's advice: without nonces, unsafe-inline means CSP does not stop script injection if an XSS ever exists; this site renders no user-supplied HTML, so nonce plumbing was rejected as over-engineering. Verified against a production build: playback, session replay and beacons run with zero violations.

## 2026-08-13: the roadmap belongs to the owner

The parking lot is gone and stays gone: agent ideas do not get stored in the owner's planning docs. Unadopted proposals die in conversation; docs/roadmap.md carries owner direction only. Same day, the previously parked scale concerns the owner did adopt were built: masthead collapse, hero name cap, share name cap.

## 2026-08-13: share surfaces carry no counts

Share platforms (Twitter, WhatsApp, Slack, iMessage) cache link previews for days or weeks, so any number on og.png or in og/twitter descriptions goes stale in the wild no matter how it is generated; even a dynamically rendered card cannot beat the platform cache. The og.png card and all og/twitter descriptions are therefore timeless: name, provider list, the sample promise, no counts. Counts stay in SEO meta descriptions and on the pages themselves, which crawlers re-fetch. The card names the blessed providers, so regenerating it (scratchpad og.html pattern) is one step of the provider blessing ritual.

## 2026-08-13: a voice is a provider voice-list entry; a sample is a playable render

The owner caught the site counting Polly's 177 per-engine rows as 177 voices while counting Gemini's per-sub-model takes as samples: the same concept, two treatments. Settled definition: a voice is one entry in the provider's own published voice list, a sample is one playable render of it, and the mapping is per provider (`voiceIdentity` in the bless config). Google's list counts every language+family+name row (en-US-Standard-A and en-US-Wavenet-A are distinct voices in Google's docs), so google stays row-counted at 4,586. AWS's Available Voices table lists a voice once per language with engines as capability columns, so polly counts distinct language+name: 113 voices, 177 samples. Kokoro is flat: 54 and 54. Cross-language name reuse is counted once per language, matching both providers' own tables (AWS lists bilingual Aditi under both of its languages; Google lists Achernar per language). Site totals became 4,753 voices, 12,647 samples. Grouping Polly's list rows by voice with a per-row engine picker was considered and parked: the flat per-engine rows keep every render directly playable and comparable.

## 2026-08-13: providers go live by blessing; the machinery is generic

The multi-provider shelf shipped with Amazon Polly (177 voices) and Kokoro (54) beside Google (4,586). Data, routing, playback and SEO surfaces are generic over a provider dimension, but a provider only goes live through the curated config in `lib/providers.ts` (copy, family metadata, the provider's own vocabulary) plus a refreshed data pack and a real browser playback pass. The bare `/voices` endpoint reports every provider upstream; unblessed ones are console-logged and stay invisible. The owner explicitly declined webhook or CI alerting for newcomers: logs are enough. Rationale: honest copy and verified playback cannot be automated, and a provider appearing on the site should never be a surprise.

## 2026-08-13: umbrella at the root, Google under /google, permanent redirects are 308

The umbrella homepage lives at `/` (provider cards, aggregate stats, no voice list). Google's whole section moved to `/google` with permanent redirects from every old URL: `/voices/:lang` to `/google/voices/:lang`, and `/` to `/google` whenever an explorer-specific query param is present (four separate `has` rules for family, language, gender, gmodel, since `has` entries AND together; `q` is excluded on review, too generic a name to bind permanently to /google). Note for the earlier subfolder entry that said "301": Next's `permanent: true` answers **308**, the method-preserving permanent redirect; search engines treat it as permanent the same way. Hash-only anchors can't be redirected server side and nothing was deployed yet, so they are not chased.

## 2026-08-13: catalog serves per-provider slices, and the process memo is the real cache

`/api/catalog/[provider]` replaced the single catalog blob: google is about 95 percent of the payload, and a visitor on /polly or /kokoro should not download 4,586 Google voices to browse 177. Separately, the all-provider upstream response (~2.9MB) is over the Next data cache's 2MB item limit, so the `revalidate` fetch cache silently stores nothing; the process memo (six hours, with a 60 second fresh floor for /api/sample id misses) is the effective catalog cache, and the pages' daily ISR sets the visible refresh cadence.

## 2026-08-13: a generous in-memory rate limit guards the one non-static route

Owner asked for a sanity limit against dumb loops racking server bills. Scope: `/api/sample` only, since every other route is static and rides the CDN. Per-IP token bucket, burst 30 then 30 a minute, 429 with Retry-After, buckets swept when idle. Deliberately not security (x-forwarded-for is forgeable, state resets on deploy) and deliberately not the guardian of the shared upstream sample-url budget; the 20 hour signed-URL cache with coalescing and the 429-to-503 relay already do that. No global limiter: rejected as over-engineering.

## 2026-08-13: name is Speakshelf, domain speakshelf.com

"Voice Atlas" collided with existing products (voiceatlas.com among others). Out of 44 checked .com candidates, 9 were unregistered; the owner picked speakshelf.com over voxroster.com and voxcensus.com. Knockout trademark glance done the same day: no US federal filing live or dead for speakshelf or speak shelf in the indexed register mirrors (nearest marks SPEAKHDL, SPEAKAIR, SPEAKME, all different goods), nothing indexed from EUIPO, no product or company anywhere with the name. A glance is not clearance: before serious brand spend, do a formal USPTO and WIPO search or have an attorney run one.

## 2026-08-13: multi-provider growth uses subfolders on one domain

When Polly, Kokoro, Azure and others arrive: the umbrella home lives at `/`, each provider under a subfolder (`/google`, `/polly`), and the current Google catalog moves to `/google` with 301 redirects for every existing URL. Not subdomains (they earn search trust separately for months) and not separate domains (start at zero, cost the most). Backed by 2026 research: Google treats subdomains as semi-separate properties; G2's own subdomain move took months despite huge site authority.

## 2026-08-13: samples resolve on demand; there is no such thing as a sample-less voice

The upstream `sample-url` endpoint is generation class: it synthesizes a missing sample on first request and answers 202 while it works. The original overnight build misread those 202s as "no sample" and shipped dash placeholders for 952 voices; all 4,586 voices proved playable. The `hasSample` flag was removed from the data format, every voice gets a play button, and the UI shows "preparing sample" with auto-retry when it catches a 202. Any future probe or refresh script must treat 202 as pending, never as absent.

## 2026-08-13: catalog is data-driven and self-refreshing

No hardcoded voice, language, family, or sub-model lists anywhere. The server refetches the live catalog daily with the committed packed file as seed and fallback (with a sanity floor). Gemini's sub-models come from `available_models`. Cost of a new Google model appearing upstream: zero code changes. This was an explicit owner requirement.

## 2026-08-13: playback caching copies tts-microutil's good half only

The owner's other product at `/Users/me/mDevs/tts-microutil` validated the pattern in production: stream the first play from the signed URL, then warm an in-memory blob cache so replays are local object URLs, which is what Safari needs. Copied: that two-step, the staleness counter, warm-after-first-play. Deliberately not copied: its four unsynchronized cache layers, the dead v2 hook, the localStorage cache written by nothing.

## 2026-08-13: the voice list renders last on the home page

The list is huge, loads client side, and re-measures under content-visibility, so anchors below it can never land reliably (measured drift of tens of thousands of pixels). Sections order: hero, families, languages, about, list. Anchor fixes via scroll hacks were tried and rejected.

## 2026-08-13: SEO scope

Ship: llms.txt (daily regenerated), robots open for exactly the two public API paths, JSON-LD WebSite and CollectionPage, sitemap, dynamic metadata. Skip on purpose: per-sample AudioObject and ItemList markup (no consumer in 2026, pure bloat), Dataset markup (scope misuse), speakable (beta, articles only). Deferred: IndexNow until the real domain exists.

## 2026-08-13: money and key rules

`POST /tts` is never called by this codebase or by any agent working on it; it spends real credits. Everything the site does is free (listing, sample-url). The API key stays server side, gitignored, and out of logs. The balance was checked before and after the entire build: unchanged at $147.64.

## 2026-08-13 (overnight build, 1am to 4:30am): foundations

IBM Carbon-inspired design system, IBM Plex via next/font, square corners, one blue. Human copy everywhere: no em dashes, no AI phrasing. Next.js App Router with SSG for language pages and a client-loaded home list. Design-first flow through a Claude Design mockup, then ported to the app.

## 2026-08-13: router scrolls opt out of smooth scrolling

Clicking between pages landed short of the top while pasted URLs landed clean. Next 16 stopped suppressing CSS `scroll-behavior: smooth` during its scroll-to-top on navigation unless the html element carries `data-scroll-behavior="smooth"`; without it the scroll became an animation whose own follow-up check cut it short around the masthead's height. The attribute now sits on the html element in the locale layout, and smooth stays for in-page anchor jumps, which never enter the router. Two guardrails for future work. Never add `scroll-padding-top` to html: the router reads it for its in-viewport check and it reintroduces the same landing-short bug even with the attribute present; per-target `scroll-margin-top` is the house pattern. And browser tests must scroll with `behavior: "instant"` in setup steps, because plain `window.scrollTo` animates under the smooth rule and a click mid-animation measures garbage.

## 2026-08-13: locale switches keep the reader's place

The suggestion strip and the footer switcher replace to the same page in the new language with `scroll: false`; a language switch is the same page, so the reader keeps their spot. Known limit, accepted: the voice list renders under content-visibility auto, so an incoming page's height starts near an estimate and grows as the browser measures it, and a reading position deeper than that early height gets clamped mid-transition (measured: deep on the Google shelf lands near y 2250). That clamp comes with every client navigation regardless of scroll options, and the clamped spot is still closer to the reader's place than the top would be. The same mechanism leaves a residue of about 11px after some navigations while the language strip is mounted; it clips only the strip's own top edge, never content.

## 2026-08-13: the app owns its navigation glide

Making router scrolls instant (earlier today) fixed the landings but killed the glide the owner wanted, and Next has no supported smooth navigation scroll: the issue asking for one (#51721) was closed by the PR that added the suppression. So the app owns the animation. The Link that everything imports from i18n/navigation wraps the next-intl Link with Next scrolling off; a plain primary-button click on it records one glide intent (destination pathname, search, hash), and a same-document destination glides immediately, which is how the wordmark at home and the active masthead tab return to top. ScrollGlide, mounted once in the locale layout behind Suspense, lets the first route commit consume the intent, and only a commit matching the intent's URL glides, animating to the hash target or the top; a mismatched commit (a superseded click, a redirect) just drops it. Back and forward, the Explorer's replaceState writes and the locale switchers record no intent, so browser restoration and keep-your-place survive untouched. That intent gate is load-bearing: installed Next 16.3 mirrors external replaceState into useSearchParams (its patched history dispatches a restore action), so a manager reacting to route keys alone would glide on every search keystroke. The html attribute and the CSS smooth rule stay: native in-page anchors keep their animation, and any navigation that slips past the wrapper lands instant and correct rather than animated and short. Readers with reduced motion get instant scrolls through a media query the glide helper also respects. Verified headless on the production build: twelve-case matrix asserting animated trajectories frame by frame, exact landings, quiet back button, kept locale-switch position, one real playback.

## 2026-08-13: counts format through ICU everywhere, and the hero names shelves like the share card

The owner caught the hero heading showing a raw 4753 while the stat cells under it grouped correctly. Root cause: bare {arg} placeholders render digits unformatted; only {arg, number} and plural # group per locale. Every bare numeric placeholder across all eight locale files now carries the number format (fourteen keys; familiesCount stays bare because the code pre-formats it). One nuance to leave alone: Spanish groups only from five digits, so "4753 voces" next to "12.647" is correct CLDR typography, not a miss.

The hero subtitle and the home JSON-LD now follow the pattern the owner set explicitly: the first three shelves as a bare comma list, each locale's message closing the list itself ("and more", "y más", "等提供商"); the subtitle starts a fresh sentence after, the JSON-LD continues after a comma. The counted "and N more" variant is gone. With exactly three shelves shipped the closing phrase already reads "and more"; review flagged that as a forward claim, and the owner kept the wording deliberately, with the fourth provider in the works. The list joins in listNamesPlain with a plain comma, enumeration comma for zh; Intl.ListFormat was rejected there because its unit type concatenates zh names with no separator and its conjunction styles double the message's own closing conjunction in es and hi.

## 2026-08-14: the language picker lives in the footer's utility corner

The owner flagged the picker sitting 16px under the wordmark: a boxed utility control glued to the brand cluster. Of three options (more air in place, a full-width Carbon settings row, or moving it to the right column), the owner picked the utility corner: brand stays alone on the left, the note keeps its left edge on the grid midline, and the language picker sits below the note. On narrow screens the stack reads brand, note, language last. The footer's zones now mean something: identity left, facts right, utility in the corner.

## 2026-08-14: the quirk toast goes Carbon top-right, and generation gets real patience

The owner asked whether the bottom-left toast was Carbon; it was not (Carbon toasts live top right), so it moved: fixed under the sticky masthead at top right with a short slide-in, reduced motion gets none, mobile full width. Display logic settled by the owner: with the family filter on "all", the once-per-session toast alone carries Gemini's accent note; the permanent inline note appears only for an explicit family choice in the dropdown, and the toast never doubles a visible permanent note (the filter is read through a ref at play time, since cold generations can start half a minute after the click).

Sample patience was redesigned after the owner caught "sample unavailable" on voices that played fine one retry later. First listens render upstream and the slow engines take well over half a minute, so the 202 chain now runs on a 90 second wall-clock budget (checked at dispatch), honors retry_after, carries a 15 second per-request abort, and treats 404s and 5xx blips mid-generation as "not yet" rather than "never": a chain that ever saw 202 can only end in "still preparing", never "sample unavailable". The route keeps a generating marker per voice for its retry interval so all retriers of a cold voice cost one upstream lookup per interval; without it, the longer client patience would have amplified into the process-wide miss budget. Live limitation recorded honestly: at review time no cold sample existed upstream to exercise the full chain in a browser, so the redesign shipped on contract, code review and the peer lane's verified findings.

## 2026-08-14: the toast goes light, and placement dodges what you tapped

Approved from mockups after the owner flagged two things on the top-right dark toast: dark on the dark hero blends away, and a fixed mobile slot can cover the row that was just tapped. The toast is now Carbon's low-contrast variant, which is also what Carbon itself prescribes for supplemental notes (high contrast is for urgent ones): light ground, 1px border, 3px blue leading edge, readable over the dark hero and white content alike. Desktop keeps Carbon's top-right slot. Mobile sits at the bottom by default and takes the top slot instead when the tapped row is in the lower half of the screen at show time, or while the undecided consent bar owns the bottom edge (consent keeps the higher layer). In the same pass the toolbar labels got the CSS uppercase house pattern, which the owner confirmed after catching MODEL FAMILY in lowercase: the family word stays a lowercase bare noun in the messages for grammar, presentation uppercases it, and the one stray JavaScript uppercase call is gone.

## 2026-08-14 (overnight): contact waits for a domain mailbox

The owner asked where a contact email should live on a site with no backend. Researched and decided jointly with the peer reviewer while the owner slept: no form vendor before the legal and domain decisions land, and no personal address published as a permanent support channel (commit metadata exposure is not the same as inviting the world to write). The item holds until speakshelf.com brings a domain alias. When it launches: a visible, copyable address plus a mailto link in the footer's right utility column, never hidden behind JavaScript as the only fallback, since obfuscation defeats screen readers before it defeats scrapers.

## 2026-08-14 (overnight): six more locales, and Arabic brings the RTL pass

The owner's list: Indonesian, Arabic, Swahili, Japanese, German, Italian, taking the shelf to fourteen locales and 2,072 sitemap URLs. Arabic supersedes the earlier RTL deferral by the owner's explicit instruction, so the functional bidi work shipped with it: dir=rtl on the html element, the direction-bearing CSS converted to logical properties (stats grid, toast placement and leading rule, select padding and caret, the push-to-end margins), the toast slide mirrored, dir=auto on the search input, Latin voice names and language codes isolated with bdi and dir=ltr, and the langPage crumb arrow mirrored inside the Arabic messages. The brand mark deliberately never mirrors: it must match the favicon in the tab above it. Arabic and Japanese load their own IBM Plex faces (preload off, activated per lang through token overrides), with Plex Mono kept first in the mono stack so ids and codes render as codes.

Grammar honesty: Arabic carries the full ICU plural forms where a small count can render, and the correct few/many/other forms everywhere (the 4,753 hero reads with the many-form صوتًا), and Polly's arb keeps its hand-localized Standard Arabic name in all fourteen locales because CLDR collapses it (the langname fixture guards this). Known and accepted after screenshots at three widths: numbers render in Western digits in Arabic, consistently everywhere, which is common Arabic web typography; the six translations are AI-written like the first seven and await the owner's review; deeper RTL aesthetic polish stays a joint review item, but the functional pass (mirroring, overflow, playback states) was verified in the browser before shipping.

## 2026-08-14: the contact address is hi@speakshelf.com

The owner lifted the hold: the domain and mailboxes are being secured, so the held contact item shipped. Address chosen jointly with the peer reviewer and locked by the owner: hi@speakshelf.com, matching the site's human copy voice; info@ was rejected as the most dictionary-guessed alias with the coldest register, support@ as overpromising a desk. The launch shape from the hold entry shipped as recorded: a visible, copyable mailto address in the footer's right utility column, labeled per locale, held LTR so it never reorders in Arabic. An unpublished info@ alias into the same box stays an option on the mail side, never on the site. Mailbox setup notes passed to the owner: SPF, DKIM and DMARC before sending, replies always from the domain address. Spam posture, settled by the owner after the trade-offs were laid out: the address stays plainly visible in HTML, bundle and repo, with mailbox-side filtering as the defense and alias rotation as the insurance; no obfuscation, no client-only rendering, no form vendor. Do not "improve" this with hiding tricks.

## 2026-08-14: deployment is Cloud Run behind Firebase Hosting, near zero at idle

The owner's requirement was usage-based cost, paying nothing in a month with no traffic, which ruled out Vercel Pro's fixed 20 dollars a month. Vercel's free plan is legitimate for the site today (no payments, ads, affiliate links or donations) but pauses resources at its ceiling instead of billing overage, and the plan choice should not depend on staying small. Researched jointly with the peer lane, both with live web access: Cloudflare as a CDN in front of Cloud Run fails on the Host header (the clean fix is Enterprise-only, Cloud Run's own domain mappings are preview and not recommended for production, and Google's load balancer has a fixed monthly cost that defeats the goal). Firebase App Hosting was rejected for now: its adapter lists support through Next 15 while this app runs 16.3 with proxy.ts, which their own blog admits is still hard for providers. What ships instead is Google's supported pairing: Cloud Run (min 0, max 1, us-central1) with classic Firebase Hosting as CDN, domain and TLS, and Cloudflare at most as DNS. Max one instance protects the process-local sample cache and rate budgets; a burst can meet a 429, which beats adding shared state nobody needs yet.

The owner's warming trick from the reference repo (a scheduled function probing task queues, streaming functions and the Next app) shrinks here to one Cloud Scheduler HTTP job hitting /api/health on the run.app URL every five minutes, with the reference's x-warm-probe marker kept and the route warming the catalog memo via getSite(). Recorded honestly as probabilistic warming. Staging is the free run.app and web.app URLs with the web.app origin baked as canonical; buying speakshelf.com triggers one rebake redeploy (review insisted, correctly, that pages must not advertise an unowned domain). The locale cookie went off in the same change: nothing read it, and its Set-Cookie blocked CDN caching of every prefixed locale. The key travels .env to Secret Manager through a checked, non-echoing pipe, with a hidden owner prompt only as fallback; the reviewer preferred owner-typed entry, the owner's CLI-autonomy instruction won. Budget alerts exist before anything can charge and notify rather than cap. The plan itself went through the peer lane three times (two blocks, then pass) before the first task ran.

## 2026-08-14: the deploy shipped, and the CDN caching question closed in our favor

Executed to staging the same day: Cloud Run service speakshelf (revision 00001, us-central1, min 0 max 1, 512Mi) behind Firebase Hosting at speakshelf-prod.web.app, secret in place, warm probe firing every five minutes and answering 200 in the service logs. The open question from planning, whether Firebase's CDN caches Next's ISR HTML when its Cache-Control says s-maxage without the word public, was measured rather than assumed: a second request to / returns x-cache HIT, so the CDN honors s-maxage as shipped. Static chunks HIT as expected under public, max-age 1y, immutable; /api/health stays a MISS by design under no-store. The response Vary includes cookie, which is why the locale cookie removal mattered: cookieless requests keep one clean cache key per URL. Verified in a real browser against the deployed site: eight-check Playwright pass including one real sample playback reaching the playing state, and screenshots at en and ar in both widths. The playback harness records the sample endpoint's 200 as network truth because headless Chromium does not reliably surface media-stack fetches; the playing button state is the playback evidence.

## 2026-08-14: speakshelf.com is live, and the CDN taught one more lesson

The owner bought the domain at Cloudflare Registrar; records stay DNS-only there because a proxied CNAME to Cloud Run fails on the Host header, Cloud Run's own domain mappings are preview, and Google's load balancer has a fixed cost. The attach ran through Firebase Hosting's REST custom-domain flow exactly as the peer lane prescribed: create, poll the operation, then apply only what requiredDnsUpdates asked (one A record and one ownership TXT for the apex, one CNAME for www), and watch the resource until ownership, host and cert all read active. www carries redirectTarget speakshelf.com and answers 301 with path and query preserved, verified. The certificate walked VALIDATING to PROPAGATING to ACTIVE in just under two hours; the "not secure" the owner saw mid-provisioning was Firebase's placeholder cert, gone once the real one minted.

The lesson: after the canonical rebake redeploy (build env NEXT_PUBLIC_SITE_URL moving from the web.app origin to the domain), the homepage still served web.app canonicals. Not a build fault, a stale CDN copy under the day-long s-maxage. A new Hosting release purges the CDN, and that is now the recorded procedure after any rebake. Verified on the live domain afterward: canonicals, head hreflang alternates and the sitemap's 31,080 alternate entries all carry speakshelf.com, og tags intact, security headers and HSTS on, CDN HIT on second requests, and the eight-check Playwright pass including one real sample playback. Same day, the owner's agent added the Speakshelf origins to the sample bucket's CORS additively (their production project, their agent, our independent verification before and after), which un-broke the deployed blob replay warm that localhost's allowlisting had hidden in dev.

## 2026-08-15: the GA id is real, and the translation review will be three-handed

The owner supplied the production GA4 measurement id (G-MBKYB7QX64), replacing the placeholder that made Google Analytics silently collect nothing; baked in via the documented rebake procedure (build env + Hosting release). And the pending translation review is now defined by the owner as a joint pass by all three: the owner, the building agent, and the peer lane together. Scheduled for later; the AI-written label on the thirteen locales stays until that pass happens.

## 2026-08-15: replays play from decoded memory, and the toast stops eating clicks

The owner's deployed-only symptom (a second play re-buffering for a second) had two stacked causes. The sample bucket's CORS allowlist knew the reference app's domain and localhost but no Speakshelf origin, so the blob warm died silently in production while dev looked fine; the owner's agent fixed that additively in their project, verified here independently before and after. And replays leaned on the media element plus the browser's HTTP cache instead of decoded memory. The fix is the reference /voices player's validated architecture, ported lean per the joint study: a decoded AudioBuffer LRU played through one Web Audio context as the first replay tier on every engine, the blob cache upgraded to keep raw bytes beside the object URL (decoding reads the stored bytes, never fetch(blob:), the reference's CSP lesson), warming moved to the confirmed-playing moment and deduplicated in flight, the active source stopped before any new play (the reference's discontinuity lesson), and one deliberate deviation the reviewer endorsed: a suspended context resumes on the replay click instead of scheduling silence. Excluded by the owner's explicit rule: the reference's prefetch system, because the shared sample-url bucket must never fund speculative fetches; also excluded its format negotiation, which belongs to their API.

Measured locally before ship: first play about 220ms from click, replay 97ms with zero sample-audio network requests, stop and voice switching clean on the Web Audio path. The verification also caught a real pre-existing bug by accident: the quirk toast is a fixed overlay above the top rows and swallowed clicks on the play buttons beneath it for its whole display time; it now passes pointer events through, with only its dismiss button clickable. The reviewer's design corrections were adopted wholesale, including his correction of the building agent's own misdiagnosis (the blob replay path existed and worked; production CORS was what starved it).

## 2026-08-15: three Arabics, a plain Javanese, and a mobile shape for the explorer

Two label defects were live on speakshelf.com/google: `ar-001` read "Arabic (world)" and `jv-JV` read "Javanese (JV)", a raw subtag in a bracket. Both were researched to a source before anything changed, and both quotes were fetched and read rather than recalled. Google's voices doc says in as many words that "ar-XA is Modern Standard Arabic (usually denoted as ar-001)" (https://docs.cloud.google.com/text-to-speech/docs/voices), and its Gemini-TTS table lists "Arabic (World) | ar-001" and "Javanese (Java) | jv-JV" (https://docs.cloud.google.com/text-to-speech/docs/gemini-tts).

Three rulings follow. `ar-001` becomes a hand-localized "Arabic (Modern Standard)" in all fourteen locales, so the shelf now carries three Arabics that must stay pairwise distinct everywhere: plain Arabic for ar-XA, Modern Standard for ar-001, Standard for Polly's arb. `ar-XA` stays plain "Arabic", which is Google's own label for those rows in its voices table. `jv-JV` renders the base language through Intl, localized in every locale for free; Google's "(Java)" bracket was read and deliberately not adopted, because it names the island, the owner never approved it, and it would need fourteen hand translations to add nothing a reader is looking for. The dead `"jv-JV": "Javanese"` override went with it: Intl rendered "Javanese (JV)" successfully, so the fallback that entry sat behind never ran, and a uniqueness-only fixture could not see it. OVERRIDES keeps its narrow meaning, an English last resort for codes Intl cannot render at all, and a blanket override-precedence change was rejected because it would leak English into the other thirteen locales. English alone also shortens "(Hong Kong SAR China)" and "(Myanmar [Burma])". The fixture now asserts exact English labels for the fixed codes, rejects the junk brackets in every locale, and checks the three Arabics pairwise: 105 codes, 14 locales, exit 0.

The mobile package is the owner's choice, taken before they left: two-line voice rows and a full-screen filter panel, boxy Carbon, with every piece of metadata surviving the narrow width. Until now the CSS deleted the family tag and the styles under 721px, so a reader could not tell Kore-Gemini from Kore-Chirp3HD (Kore ships in both families), and five filter boxes stacked into a wall above the list. The toolbar now keeps the search field alone, a FILTERS button beside the result count opens the panel, and the row's second line carries family, the effective Gemini sub-model, styles, the trait tag and gender. The sub-model appears only on mobile rows because it is Explorer state rather than voice data: the desktop reader already has the picker in view beside the list. The panel is a hand-rolled dialog, no focus-trap or sheet dependency, and it saves and restores the body's prior overflow rather than assuming it was empty. The desktop layout from 721px is preserved: the shape's layout rules live in media queries and the panel opens only from the mobile FILTERS button, the fields are one component mounted twice with an id prefix each so no duplicate ids reach the DOM, and a window widened past the breakpoint closes the panel instead of leaving a modal over the toolbar.

The iOS zoom fix shipped with it is deliberately small: `.search-input` and `.select` reach 16px under a mobile-or-coarse-pointer condition, and nothing else grows. The coarse-pointer half deliberately reaches wide touch screens too, where the same focus zoom applies. A viewport maximum-scale hack was rejected because it disables pinch zoom for everyone, and a global input font rule was rejected as a wider blast radius than the defect.

Three new UI strings landed in all fourteen locales (the FILTERS button, the panel title, the closing button's count), beside the fourteen hand-written ar-001 names. Both sets are AI-written and pending the owner's review like the rest of the translations, so they join the three-handed pass recorded above.

## 2026-08-16: brackets for every language, one name for the Google twins, two mobile refinements

The owner overruled parts of yesterday's label round on looks, which is theirs to set. The 2026-08-15 entry above stays as written, as history. Four rulings. Every language label carries a bracket, in every locale, so the list reads as one shape instead of a mix of bracketed and bare names; the hand-written names gained theirs, Arabic's own included, "العربية (الفصحى)" for Polly's arb and "العربية (الفصحى الحديثة)" for Google's register. Bracket text stays short, so "(United Arab Emirates)" joins US, GB, Hong Kong and Myanmar in the English shortening list and ar-AE reads "Arabic (UAE)". Google's own "(Java)" is adopted for jv-JV after all, by the owner's explicit choice, hand-localized in all fourteen locales because the island has a name in each; Indonesian reads "Jawa (Jawa)" for the same reason CLDR itself reads "Indonesia (Indonesia)" there. And ar-XA and ar-001, one register under two codes by Google's own documentation, now read the same name everywhere.

Two shapes were offered for the twins and the owner picked one name with two sections, not one merged section. The codes do the telling apart on the surfaces they chose: the mono code chip already printed beside the name on group heads and language page heroes, a code suffix on the Explorer's dropdown option (added only when another code of that provider localizes to the same name, and wrapped in first-strong isolation so an Arabic option line cannot reorder it), and the code inside each twin page's metadata title, share title and JSON-LD name, so a search result or a share card is never ambiguous. The visible heading, hero and prose stay as they were. One surface was added in the reviewer pass rather than by the owner's list: the provider page's language grid showed the twins as two identical cells, so twin cells now print their code too, an extension in the ruling's spirit. The same pass retired the last English fallback: en-GB-WLS, which Intl cannot render in any locale, previously fell back to the English "English (Wales)" in all fourteen interfaces while the code claimed English could never leak; it is now hand-localized like the other exceptions and the claim became true. Polly's arb keeps its own "Arabic (Standard)": the umbrella home page names it as a Polly-versus-Google variant, so folding it into the Google name would make that sentence untrue.

The fixture grew to match, and every new assert was checked by mutation before it was trusted. Brackets are asserted in all fourteen locales: at least one pair, balanced, non-empty, ASCII or fullwidth. Nested pairs are legal outside English because CLDR nests there itself (ru "Мьянма (Бирма)", es "Myanmar (Birmania)"), while English asserts exactly one pair capped at 16 characters, since the CLDR long form that started this round was 19 and the longest sanctioned English contents today are "Modern Standard" and "North Macedonia" at 15. The collision allowance is group exact: a shared name passes only as the complete group {ar-001, ar-XA}, so a third code taking that name still fails. The twins are asserted equal in every locale and distinct from arb and from ar-EG. 105 codes, 14 locales, exit 0.

Two mobile refinements, also the owner's. The voice row's first line now reads the way the desktop row does, name, styles, gender at the line end, and the second line carries the model story, family tag, sub-model and trait tag, so the eye meets the voice before the machinery. The FILTERS button fills its own row with the count and clear-all wrapping below it, because at that width it is the only way into the filters. One implementation lesson for whoever touches these rows next: the line break is a full-width pseudo element on the row, and it carries the 4px gap itself while the row gap is set to zero. A break line of its own sits between two row gaps and doubles the spacing, and a negative margin cannot cancel one of them, because a flex line's cross size does not go below zero; measured in Chromium at every margin from -2px to -6px, the row height did not move off 79px until the gap was moved onto the break.

## 2026-08-16: the independence line goes hybrid, generic where it is shared and named where the brand is

The named list ("not affiliated with Google, Amazon or the Kokoro project") sat hardcoded inside translated prose, twice over in each of the fourteen locales: once in the about paragraph, once in the footer. Blessing a provider therefore cost twenty eight hand written sentences before it could go live, the two copies could drift apart, and at twenty providers the sentence stops being a disclaimer and becomes a wall. The owner chose a hybrid rather than going fully generic. Shared surfaces, the home about paragraph, the footer on every page and llms.txt, carry a generic line; each provider page names its own provider and only that one. The trade off is deliberate: a named list is the clearest form of trademark disclaimer, so the named form stays exactly where each brand is strongest, on that provider's own page, where naming one provider is both shorter and more precise than naming all of them. The shared surfaces give up that precision for a sentence that survives the twentieth bless.

The shape: `attribution.body` is retired for three keys. `attribution.independence` is the generic sentence, `attribution.independenceNamed` is the named one with the bless config label interpolated as `{name}`, and `attribution.source` carries the rest of the old paragraph unchanged, the AI TTS Microservice link and the on demand sentence. The home about renders independence then source inside one paragraph, the provider about renders independenceNamed with `name` set to `meta.label` then the same source. Two independence keys rather than one grammatical template with an optional argument was the reviewing lane's call, and it is the right one: a single template would have to bend fourteen grammars at once. The interpolated label is invariant: the label itself is never altered or translated, while adjacent grammatical particles are permitted. That is why the German reads "nicht mit {name} verbunden", the Japanese "{name} とは無関係です" and the Hindi "{name} से संबद्ध नहीं है", and why Bengali may attach its genitive as "-এর" after the untouched label, the way Bengali writes Latin names. The footer's served clause went generic in all fourteen and kept its link half. The AGENTS.md ground rule now records both forms, and blessing a provider no longer touches a name list in copy anywhere: the generic surfaces need no edit and the provider page reads its label from the bless config.

Verified against a production build, 73 of 77 chromium checks green: the generic line and the generic footer on the English home page and on all three provider pages, the named line carrying the right label on /google, /polly and /kokoro, the named line in all thirteen other locales on /google with "Google Cloud" verbatim inside each translation, the generic line and the reworked footer on all fourteen home pages, and the llms.txt line. No sample was played and no sample-url was spent.

The four red checks are one defect that predates this round and that this round did not touch: French and Italian both put an apostrophe immediately before the `<link>` tag ("de l'<link>", "dall'<link>"), and ICU reads that apostrophe as the opening of a quoted literal, so those two locales print the raw tag text instead of a link and swallow the apostrophe that closes the quote further along the sentence. The same words render the same way on speakshelf.com today, in the about paragraph and in the footer, and a scan of all fourteen files found the pattern in exactly those four strings and nowhere else. The review pass fixed it in the same round rather than shipping a known-broken string: ICU renders a doubled apostrophe as a single one, so the four strings now read "l''<link>" and "dall''<link>", and the French and Italian pages render the link as a real link with every apostrophe intact, verified in the browser before deploy. One operational lesson rode along: the first deploy of this round shipped the broken strings anyway, because a source deploy snapshots the working tree at upload time and a verification agent was concurrently reverting and restoring those very files in the same checkout; the tree looked clean before and after, only the snapshot in between was poisoned. Rule since: nothing may mutate the working tree while a `gcloud run deploy --source` upload is in flight, and the live page is re-verified against the exact strings after every deploy.

## 2026-08-16: the owner owns every issue, and the parked list gets drained

The owner set a standing rule: own every issue you find, fix it after discussing it, never park it as out of scope, no blame. This entry records the first drain under it. Five things had been seen and left: a 1,756 line stylesheet against a 1,000 line cap, three eslint errors and seven warnings, unmatched URLs serving a bare unbranded 404, and two Arabic bidi quirks. Everything below was re-verified on the day rather than taken from the earlier notes.

The 404 was the one the earlier note had wrong. A branded, translated not-found page already existed at `app/[locale]/not-found.tsx` with keys in all fourteen locales; unknown one-segment paths never reached it because `/[provider]` exported `dynamicParams = false`, so Next answered at routing before any layout ran. The first cut dropped that export so the page's own `notFound()` could fire; the reviewer then proved from Next's own response-cache source that an ISR route persists an on-demand `notFound()` as a cached negative entry per garbage URL, which on Cloud Run's tmpfs is memory, so the export came back. The shape that shipped: `dynamicParams = false` stays, and a new `app/[locale]/[...rest]/page.tsx` catches everything the named routes decline, one-segment and deep alike, as a dynamic render that caches nothing and reaches the branded page (verified: garbage paths 404 branded while `/google` stays prerendered). The language route keeps `dynamicParams = true` out of necessity, a refresh's new language must render on first request, and its garbage-under-`/voices/` negative entries are accepted as small, daily-expiring and instance-local. Nothing was redirected and no message key was added. Two limits are recorded rather than papered over. Next 16.3 serves a `notFound()` from a rendered route as its own `<html id="__next_error__">` document with an empty body, with the branded markup arriving in the RSC payload and rendering on the client, so a reader without JavaScript sees a blank 404; that was already true of the language pages and the status code and the injected `noindex` are correct either way. And a URL with a dot in it is outside the proxy matcher, so it reaches `/[locale]` with a non-locale segment and the root layout's own `notFound()` throws with nothing above it to catch it; those keep Next's bare page. `global-not-found` would be the only cure and was rejected: an experimental flag, a second full HTML document, a second stylesheet import against the one-import invariant, and static English under a fourteen-locale site, all to cover paths shaped like files.

The stylesheet became a manifest. `app/globals.css` now holds no rules at all: it imports `app/styles/base.css` (637 lines), `app/styles/explorer.css` (584) and `app/styles/site.css` (532) in the order they were already written, and it stays the only stylesheet the root layout imports. The cut followed the sheet's own contiguous regions and moved nothing: concatenating the three files reproduces the original byte for byte, minus the header comment that moved to the manifest, and that was checked by script rather than by eye. Import order is the cascade, so the manifest says so in a comment and the invariant is now in architecture.md. Computed styles for ten diverse selectors and screenshots at three widths in both directions were compared before and after to prove the cascade did not move.

Two React effects were rewritten without touching what they decide. `LanguageSuggest` and `ConsentBanner` both server-render, so a lazy state initializer reading `localStorage` or `navigator` would have made the first client render disagree with the server's; instead each keeps its null first render and defers the read into a cancellable zero-delay callback inside the effect. The suggestion algorithm is unchanged to the letter: a stored choice wins and is offered again when it differs from the page, otherwise the first supported browser preference ends the scan whether or not it matches, `ss-lang-dismissed` keeps its write-only meaning, and only a real offer is ever set so the two buttons keep sole ownership of clearing. The reviewing lane argued a scoped `eslint-disable` would be the better shape, since the rule is a false positive for a deliberate one-time client read; the owner's plan called for the deferral and the deferral shipped, with the dissent recorded here.

The playback clock now starts at the tap: the row passes its own click timestamp into `play()`, which uses it for the ninety second patience budget. The honest version of the benefit is small, because `play()` already ran synchronously inside the click handler, so the budget already started within a millisecond of the tap; what the change really buys is that the clock is read where it belongs and the purity rule stops flagging component-scope `Date.now()`. The URL cache's age check and its timestamp deliberately stay on real wall clock, because a URL fetched half a minute after the tap must not be stamped as though it were fetched at the tap. The four resolve tiers are untouched.

Both bidi fixes are presentation only. Each style term on a voice row is now its own `bdi` with the separators outside the isolates, so a list of terms cannot merge into a single run; no voice in today's catalog carries more than one style, so this is correct-by-construction rather than a visible repair, and it was proved against a synthetic multi-term node rather than claimed. The sub-model select wraps its rendered option text in an explicit LTR isolate (safer than first-strong for labels that open with digits), which is what stops a closed Arabic select reading "2.5 Flash" as "Flash 2.5"; `modelLabel` itself stays bare, because it also feeds the search index and the mobile row's model text, and search documents are now asserted to contain no isolate characters at all while "2.5 pro" and a raw model id both still filter.

Lint reached zero honestly. The locale layout's unused `ta` and `fmt` are gone, `applyConsent` keeps its typed parameter and consumes it with `void` so the seam's signature survives until legal fills it in, and the masthead favicon keeps a one-line scoped disable carrying its reason, because a prose comment cannot produce a zero-warning run. Two warnings nobody had named came with the round: the unmount teardown now aliases the ref objects rather than snapshotting their values, since it must act on whatever is playing at unmount, and the empty search document moved to module scope instead of joining a dependency array where it would have defeated the memo.

Two pre-existing defects surfaced in surfaces this round touched, and both are recorded rather than quietly fixed. The branded 404 pages carry two robots directives, the locale layout's site-wide `index, follow` and Next's own injected `noindex`; the restrictive one wins and the 404 status settles it anyway, so nothing is broken, but the pair is untidy and cleaning it means changing every page's robots metadata, which is the owner's call. And gtag posts one beacon to `https://www.google.com/g/collect`, a host `connect-src` does not list, so Chromium refuses it on a production build and those GA4 hits are dropped; the fix is one more CSP host, and widening the allowlist to that whole origin is also the owner's call. The earlier claim in architecture.md that every event beacon was clean of violations has been corrected in place.

## 2026-08-16: GA4 beacons unblocked, the owner widening connect-src

The round's cleanup surfaced the finding: every GA4 hit had been dropped in production since the real measurement id landed, because gtag posts to https://www.google.com/g/collect and connect-src did not carry that origin (the CSP had been verified earlier against a dummy id that fired no beacons, so nothing ever violated). Offered the path-scoped entry as the tightest opening, the owner chose the whole https://www.google.com origin instead, for robustness against Google's beacon routing. Mixpanel was never affected. Verified after the change: a production build serves the widened header and page loads emit no CSP violation naming google.com.
